/PRIVMSG <target> :<message>

Sends a notice to a target, where <target> can be
a comma separated list of one or more nicks or channels.

Parameter <target> can be @<channel> to send a message
to all channel operators (same as WALLCHOPS)

Parameter <target> can be in the form of <nick>@<server>
to send to a service.

See Also: NOTICE
